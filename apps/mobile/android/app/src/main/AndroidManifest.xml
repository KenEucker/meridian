<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
      Permissions (M19.21).

      Only the native permissions a shipped surface asks for, and each one
      names that surface. The declared set must match the code-defined
      inventory in ../src/nativePermissions.ts exactly — the config tests fail
      the build on any permission declared here without an inventory entry, or
      inventoried without a declaration.

      INTERNET: every Meridian Field surface talks to a Meridian node over the
      network (technical spec 3.3).

      CAMERA: the staff.workstation-code surface (M18.61) scans workstation
      sign-in QR codes through getUserMedia in the WebView, decoded by the
      browser's BarcodeDetector. A denied or absent camera reaches the typed
      short-code fallback, so the camera feature itself is not required.
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
