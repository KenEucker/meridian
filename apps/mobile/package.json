{
  "name": "@meridian/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Meridian Capacitor mobile packaging wrapper for the shared Vue client.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "corepack pnpm --filter @meridian/client run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "cap:copy": "corepack pnpm --filter @meridian/client run build && cap copy",
    "cap:sync": "corepack pnpm --filter @meridian/client run build && cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
