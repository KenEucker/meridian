VITE_MERIDIAN_API_BASE_URL=http://127.0.0.1:8000
# Run `corepack pnpm run env:local` from the repository root to generate
# ignored `.env.*.local` files for every Vite UI mode.
#
# There is no development-session switch any more (M18.9). A client that has not
# signed in shows the sign-in screen; sign in against a seeded node and the
# client shows that node's own staff, events, and departments.
