# Caddy Deployment

Future location for Caddy reverse proxy and certificate configuration.

This directory is intentionally empty for `M0.1`. HTTPS, local discovery, and event-mode certificate behavior are deferred to later Alpha 1 tasks.
