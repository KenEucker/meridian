# DNS Deployment

Future location for DNS configuration supporting on-site deployments.

This directory is intentionally empty for `M0.1`. DNS setup, local discovery, and on-site networking behavior are deferred to later Alpha 1 tasks.
